{
  "name": "@why-is-this/core",
  "version": "0.1.0",
  "description": "Core trace engine for why-is-this — git blame, fear scoring, rationale files, survivorship checks",
  "keywords": ["git", "blame", "rationale", "fear-score", "developer-tools"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Mukesh7522/Why-is-this.git",
    "directory": "packages/core"
  },
  "engines": {
    "node": ">=18"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "jest",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "simple-git": "^3.22.0",
    "@octokit/rest": "^20.0.0",
    "@why-is-this/embeddings": "*"
  },
  "devDependencies": {
    "jest": "*",
    "ts-jest": "*",
    "@types/jest": "*",
    "typescript": "*"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "roots": [
      "<rootDir>/src"
    ]
  }
}
